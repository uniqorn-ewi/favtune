<%= form_with(model: radio_station, local: true) do |form| %>
  <% if radio_station.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(radio_station.errors.count, "error") %> prohibited this radio_station from being saved:</h2>

      <ul>
      <% radio_station.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
