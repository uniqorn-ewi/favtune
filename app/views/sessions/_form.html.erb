<%= form_with(model: session, local: true) do |form| %>
  <% if session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(session.errors.count, "error") %> prohibited this session from being saved:</h2>

      <ul>
      <% session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :new %>
    <%= form.text_field :new, id: :session_new %>
  </div>

  <div class="field">
    <%= form.label :create %>
    <%= form.text_field :create, id: :session_create %>
  </div>

  <div class="field">
    <%= form.label :destroy %>
    <%= form.text_field :destroy, id: :session_destroy %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
